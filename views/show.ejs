<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%=fruit.name%>
    </title>
</head>

<body>
    <h1>
        <%=fruit.name%>
    </h1>
    <p>This fruit is <%=fruit.color%>
    </p>
    <% if(fruit.readyToEat){ %>
        <p>
            This fruit is ripe and ready
        </p>
        <%} else{%>

            <p>Give it some more time, its not quite ready yet</p>
            <% } %>
                <a href="/fruits/<%= fruit._id %>/edit">Edit this fruit</a>
                <form action="/fruits/<%=fruit._id%>?_method=DELETE" method="POST">
                    <button type="submit">Delete this Fruit</button>
                </form>
                <a href="/fruits">Back to All fruits</a>

</body>

</html>